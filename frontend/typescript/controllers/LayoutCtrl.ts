/// <reference path="../_ClientTypes" />

namespace ram {
    export class LayoutCtrl {
        public static $inject = [
            "$scope"
        ];

        constructor(
            private $scope: IRamScope
        ) {
        }
    }
}
