/// <reference path="../../_all.ts" />

module ram {
    export class Page2Ctrl {
        public static $inject = [
            "$scope"
        ];

        constructor(
            private $scope: IRamScope
        ) {
        }
    }
}
